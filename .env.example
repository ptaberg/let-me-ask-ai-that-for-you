# Public canonical origin used in metadata (Open Graph URLs, robots host).
# Optional — on Vercel `VERCEL_URL` is used automatically; locally falls back to http://localhost:3000.
# Set this to your production domain (no trailing slash). Example:
#   NEXT_PUBLIC_SITE_URL=https://askaifor.you
NEXT_PUBLIC_SITE_URL=
